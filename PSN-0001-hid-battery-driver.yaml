---
title: PSN-0001 — Magic Mouse HID Battery Reader Driver Binding
type: problem-session-note
psn_id: PSN-0001
version: 2.3.0
status: active
linked_prd: PRD-184
linked_repo: ReviveBusiness/magic-mouse-tray
linked_issues: [2, 3, 4]
opened: 2026-04-17
last_updated: 2026-05-09
sessions_logged: 19
close_out_gate: ".ai/playbooks/autonomous-agent-team.md#per-phase-close-out-gate-non-skippable"
session_protocol: |
  Every session that touches PRD-184 work MUST end by running through the
  5-block per-phase close-out gate (Block 1 = verification artefacts,
  Block 2 = update this PSN + plan + playbook, Block 3 = cross-reference
  linked_issues, Block 4 = /prd update-progress, Block 5 = atomic commit).
  This is non-skippable per the playbook. See close_out_gate field above.
---

# PSN-0001: Magic Mouse HID Battery Reader — Windows 11 Driver Binding

## Problem Statement

Apple Magic Mouse v2/2024 on Windows 11 requires the `AppleWirelessMouse64` HID driver to be bound to read battery via HID Input Report `0x90` (byte[2] = battery %). The driver doesn't reliably bind after suspend-resume or after Windows updates. PRD-184 delivers the core battery reader, but GitHub Issues #2, #3, #4 track ongoing driver binding reliability problems.

## Current Working Theory

The v3 mouse battery problem is a **HidBth runtime descriptor cache** issue, not a filter binding issue. HidBth serves one of two cached descriptor variants for the v3 BTHENUM HID PDO, with `applewirelessmouse` filter on the stack in BOTH cases:

- **Descriptor A (split):** vendor 0xFF00 TLC enumerated as `…&col02#…` child PDO. `HidD_GetInputReport(0x90)` byte[2] returns battery %. Empirically read 96 times on 2026-04-27 06:04-19:43.
- **Descriptor B (unified):** COL02 stripped, single Mouse TLC declares phantom Feature 0x47 with UP=0x06/U=0x20. `HidD_GetFeature(0x47)` returns err=87 because the device does not back that ReportID. Empirically: ~588 probe attempts across 6 channels (DEVPKEY, WMI, IOCTL 0x800-0x830, HidD_GetFeature/GetInputReport every ReportID 0x01-0xFE, orphan COL02 direct open, Battery class) → 0 hits.

Registry on `Enum\BTHENUM\…\Device Parameters` is byte-identical between A and B states. The state difference lives in HidBth's kernel-pool descriptor cache, not in any registry value. PnP recycle (Disable+Enable BTHENUM HID PDO) CAN restore Descriptor A but is non-deterministic — both A and B can come out the other side. A DSM (DeviceSetupManager) property-write event at 2026-04-27 19:50:53 correlates with the only observed A→B flip; mechanism unconfirmed.

Tray code is correct (`splitVendorBattery` + `unifiedAppleBattery` paths both implemented and switch on runtime descriptor caps). The bug is keeping the device in Descriptor A.

## Hypotheses

| ID | Hypothesis | Status | Date Tested | Evidence |
|----|-----------|--------|-------------|---------|
| H-001 | ReportDescriptor access indicates driver bound | **REJECTED** | 2026-04-21 | False negative — descriptor can be null even when bound due to timing. See peer-review 20260421 finding #2 |
| H-002 | worst-state-wins semantics for DriverHealthChecker returns correct binding state | **CONFIRMED** | 2026-04-22 | PR fix/issue-4-driver-priority-v2, commit cb49ece. Multiple HID devices share same VID — any unbound device = broken state. |
| H-003 | pnputil /restart-device safe to call unconditionally after driver install | **REJECTED** | 2026-04-26 | /restart-device strips COL02 (same as /disable+/enable with filter active). Removed from startup-repair.ps1. |
| H-004 | Writing LowerFilters to driver instance key (Control\Class\{GUID}\NNNN) resolves error 1077 at boot | **REJECTED** | Prior sessions | "LowerFilters in BOTH keys + DEMAND_START" tested — error 1077 persisted. PnP not loading filter regardless of registry location. Root cause unknown. KMDF driver required for scroll. |
| H-005 | applewirelessmouse filter binding is unreliable across reboot | **REJECTED** | 2026-04-27 (Cell 1 + Phase 3) | Registry binding (LowerFilters) survives reboot. The runtime BEHAVIOR (Win32-layer wheel-event synthesis) becomes inert post-reboot despite filter being loaded. Real failure mode is runtime, not binding. |
| H-006 | Scroll path requires applewirelessmouse to inject wheel events | **CONFIRMED** | 2026-04-27 (Phase 3) | Cache has NO Wheel usage. applewirelessmouse runs Win32-layer input injection (Linux hid-magicmouse.c pattern) -- not descriptor augmentation. When filter inert, no Wheel exists anywhere in the stack. |
| H-007 | Q7 yes/no -- can scroll+battery ship without a kernel driver? | **CONFIRMED YES via Phase 4A** | 2026-04-27 (Phase 3 + Experiment A) | Cache has battery TLC (Report 0x90 on COL02 vendor TLC UP=0xFF00 U=0x14) -- works without filter. Scroll requires either (4A) userland gesture-to-wheel daemon reading raw multi-touch from Report 0x12 + SendInput WM_MOUSEWHEEL, or (4C) cache patch with Wheel + AC-Pan items added to Mouse TLC. Phase 4-Omega alone does NOT enable both -- it only restores State A (scroll works, battery N/A). |
| H-009 | A simple PnP recycle (Disable+Enable BTHENUM) restores filter to active state from inert | **CONFIRMED** | 2026-04-27 (Experiment A) | Recycle reliably flips State B -> State A in ~5 sec. 65-min idle persistence monitor confirmed State A holds without immediate degradation. Tray app can self-heal by detecting State B and triggering recycle via existing MM-Dev-Cycle queue. C# prototype committed in e323df6. |
| H-008 | applewirelessmouse traps Feature reports as scroll-synthesis mechanism | **REJECTED** (was CONFIRMED-PARTIAL) | 2026-04-28 (Session 11, E6+E7c source/static analysis) | Inferred from a single error code (err=87) and a misleading log-string (AP-19). Reality: in Descriptor B the device simply doesn't back the phantom Feature 0x47 ReportID. No trap. `applewirelessmouse.sys` binary has no PID 0x0323 hardcoded — INF over-matched. Filter is bound to v1+v3 in BOTH Descriptor A (battery readable) and Descriptor B (battery unreadable) per `DEVPKEY_Device_LowerFilters`. The earlier "filter inert post-reboot → battery succeeds" framing came from a Cell 1 reading where the filter-binding probe used the wrong registry path (AP-18). |
| H-010 | v3 HidBth descriptor cache has two variants ("Descriptor A split" vs "Descriptor B unified"); modes are MUTUALLY EXCLUSIVE in the applewirelessmouse filter | **CONFIRMED + REVISED 2026-04-28 PM + RE-REVISED 2026-05-06** | 2026-04-28 (Session 11), revised 2026-05-06 (Session 16) | Two empirically-distinct HIDP_CAPS readings observed for the same v3 BTHENUM HID PDO with the same filter on stack. Descriptor A: 2 child PDOs (COL01+COL02), InLen=8/3, FeatLen=65/0, vendor 0xFF00 TLC visible, battery readable. Descriptor B: 1 child PDO, InLen=47, FeatLen=2, phantom Feature 0x47 cap, vendor TLC stripped. Registry under `Enum\BTHENUM\…\Device Parameters` is byte-identical between states — kernel-pool cache, not registry. **REVISED 2026-04-28 PM (AP-21)**: modes are mutually exclusive on this system — Mode A delivers battery but does NOT synthesize scroll; Mode B delivers scroll but strips battery. The 96 OK battery reads on 04-27 happened in Mode A; scroll was likely NOT working in that window. **RE-REVISED 2026-05-06 (Session 16 Ghidra trace)**: the "in the applewirelessmouse filter" framing is WRONG. Apple's filter is **passive** with respect to mode selection — it unconditionally injects the same single 116-byte descriptor (file offset 0xA850, RVA 0xC050) via two SDP completion paths in the F3 callback at VA 0x14000a440 (file offsets 0x9611 length-recompression + 0x96e5 drop-in replace). Both paths copy via 7 SSE 128-bit `movups` loads + 1 DWORD load = 116 bytes. **Mode A vs Mode B is a HidBth caching artifact downstream of Apple's filter, not a filter decision.** Patch leverage exists at file 0xA850 (the descriptor bytes). See `docs/SESSION-16-GHIDRA-DESCRIPTOR-TRACE.md`. |
| H-011 | DSM (DeviceSetupManager) property writes trigger Descriptor A → B flip | **CONFIRMED-PARTIAL** | 2026-04-28 (Session 11) | Event-log correlation: 2026-04-27 19:43:45 last OK battery read (Descriptor A) → 19:50:53 DSM "serviced" Magic Mouse container fbdb1973-… and wrote 35 properties → 19:51:00 DSM serviced again → 19:51:45 DSM shut down → 20:13:45 first FEATURE_BLOCKED (Descriptor B). Selective Suspend hypothesis is partially-valid: trigger is DSM property-write → PnP re-enumeration → descriptor re-fetch in whatever state the device happens to be in at that moment. Causation not yet proven; correlation is one data point. |
| H-012 | A targeted PnP recycle (BTHENUM Disable+Enable) deterministically restores Descriptor A on v3 | **NOT YET TESTED — RE-PRIORITIZED 2026-05-06** | 2026-04-28 (Session 11), re-prioritized 2026-05-06 | Empirical evidence so far: recycle on 2026-04-27 17:43:44 landed Descriptor A (96 OK reads followed). Recycles on 2026-04-27 19:54+ (persistence-monitor) sometimes landed Descriptor B. Deprioritized 2026-04-28 because Mode A would break scroll. **RE-PRIORITIZED 2026-05-06**: PRD-PATH-B M0 gating milestone makes this the critical primitive — tray app would recycle on demand to read battery, accept brief scroll glitch. Test harness deliverable: 100-attempt recycle from Mode B steady state; classify outcomes; report success rate. PASS ≥70% / PARTIAL 30-70% / FAIL <30% verdict thresholds. See `docs/PRD-PATH-B-USERLAND-RECYCLER.md` M0 section. |
| H-019 | applewirelessmouse.sys IOCTL_BTH_SDP_SERVICE_SEARCH_ATTRIBUTE handler is at VA 0x14000a440 (F3) and unconditionally injects the 116-byte descriptor at file 0xA850 via SSE 128-bit loads | **CONFIRMED** | 2026-05-06 (Session 16, capstone static disassembly) | Two patch sites in F3 SDP completion callback: file 0x9611 (length-recompression path) + file 0x96e5 (drop-in replace path). Both copy 116 bytes via 7 × `movups xmm, xmmword ptr [rip+disp]` (16 bytes each) + 1 × DWORD load (4 bytes) = 0x74 = 116 bytes. Session 15's "ZERO references" claim was wrong — my scanner only matched `48 8d` LEA and `48 b8` MOV imm64; SSE-load patterns weren't searched. Apple's filter has NO Mode A/B branching — it always injects the same bytes. Mode mutual exclusivity (H-010) lives in HidBth's downstream cache, not Apple's filter. **Implication**: PATH-A (binary patch the descriptor at 0xA850) is viable. Length must remain exactly 116 bytes unless code patches at 0x9611 + 0x96e5 are also done. See `docs/SESSION-16-GHIDRA-DESCRIPTOR-TRACE.md` for full evidence. |
| H-013 | MU kernel filter `MagicMouse.sys` alone (without userland `MagicUtilitiesService.exe`) is sufficient for scroll and battery | **CONFIRMED FAIL** | 2026-04-28 (Session 12) | Kernel-only MU install test: with trial-expired userland (service dead), scroll was broken on both v1 and v3; battery was hidden. MU's kernel filter (`MagicMouse.sys`) does descriptor mutation only — scroll translation and battery query are **userland-gated** through `MagicUtilitiesService.exe`. Architecture: kernel = descriptor mutation; userland = HID-report translation + license enforcement. Captures archived at `D:\Backups\MagicUtilities-Capture-2026-04-28-1937\` (41 files, 78.6 MB). HID descriptor dumps: `.ai/test-runs/2026-04-27-154930-T-V3-AF/hid-descriptor-full-{kbd-col01,v1-col01,v3-col01}.txt`. |
| H-014 | M12 must implement BOTH descriptor mutation AND in-IRP translation in pure kernel (no userland split) | **APPROVED architecture** | 2026-04-28 (Session 12) | Direct consequence of H-013 CONFIRMED FAIL. Because MU's userland-gated approach is unacceptable (trial expiry, EULA, maintenance debt), M12 must replicate both functions in one pure-kernel filter: Mode A descriptor mutation (already planned) + in-IRP scroll/battery translation derived from Linux `hid-magicmouse.c` + Ghidra analysis of `MagicMouse.sys`. Estimated ~300-500 LOC pure kernel filter. Decision D-016. |
| H-015 | SDP descriptor injection alone (without gesture processing) is sufficient for scroll to work | **REJECTED** | 2026-04-30 (Session 13) | COL01 enumerated correctly (Status: Started) via our combined 135-byte descriptor. Two-finger scroll on device surface produced zero scroll events. Raw touch reports (RID=0x27) are received by the HID stack but require a gesture translation layer to synthesize RID=0x02 Wheel/AC Pan values. Descriptor injection is a prerequisite, not a sufficient condition. |
| H-016 | applewirelessmouse.sys provides scroll gesture processing that can coexist with our descriptor injection | **REJECTED** | 2026-04-30 (Session 13) | Filter order swap test (commit ee18af4): placed applewirelessmouse at index 0 (closest to hardware) so it sees raw HID read completions first. Result: CONFIRMED gesture processing is active (touch produces click events), but gesture output is WRONG — spurious left-clicks on surface contact. Root cause: applewirelessmouse's gesture engine initializes state against its own injected descriptor (scroll-only or battery-only), then our driver overwrites with the combined descriptor. The byte layout mismatch causes applewirelessmouse's gesture output bytes to land in the button bit field → clicks instead of scroll. Cannot coexist with our combined descriptor. |
| H-017 | The SDP IOCTL for HID descriptor negotiation is IRP_MJ_DEVICE_CONTROL (not INTERNAL_DEVICE_CONTROL) | **CONFIRMED** | 2026-04-30 (Session 13) | Ghidra RE of FUN_14000A440 in applewirelessmouse.sys confirmed IOCTL_BTH_SDP_SERVICE_SEARCH_ATTRIBUTE (0x410210) dispatches as IRP_MJ_DEVICE_CONTROL. Previous M12 driver used only EvtIoInternalDeviceControl and never intercepted the SDP query. Adding EvtIoDeviceControl produced IoctlInterceptCount=2, SdpPatchSuccess=1 on first live device test. |
| H-018 | WDF filter driver with parallel default queue requires explicit EvtIoDefault to pass non-IOCTL IRPs | **CONFIRMED** | 2026-04-30 (Session 13) | Without EvtIoDefault, WDF with WdfIoQueueDispatchParallel completes unhandled READ/WRITE with STATUS_INVALID_DEVICE_REQUEST rather than forwarding them. This breaks the HID input path (no touch data ever reaches HidBth.sys). EvtIoDefault send-and-forget is the documented WDF lower-filter pattern. |
| H-020 | MouseClass DN_STARTED (`IsV3MouseClassPresent()`) is a valid Mode B discriminator — mouhid.sys only stays DN_STARTED on the Mouse class device when Mode B is active | **REJECTED** | 2026-05-07 (Session 17) | mouhid.sys remains bound to col01 (DN_STARTED=True) even in Mode A. `IsV3MouseClassPresent()` returns True in both modes — not a Mode B signal. All apparent "1-3ms WaitForModeB" timings were false positives from this gate firing immediately regardless of mode. Replaced by H-021. |
| H-021 | `DEVPKEY_Device_Stack` on the v3 BTHENUM devnode contains 'applewirelessmouse' only when Mode B is active — this is the authoritative runtime discriminator | **CONFIRMED** | 2026-05-07 (Session 17) | `CM_Get_DevNode_PropertyW` (`cfgmgr32.dll`, EntryPoint `CM_Get_DevNode_PropertyW`) reads `DEVPKEY_Device_Stack` (`{a45c254e-df1c-4efd-8020-67d146a850e0}` pid=14) from the v3 BTHENUM devnode. In Mode A: 'applewirelessmouse' absent. In Mode B: present. Property is runtime kernel state — only populated after FLIP:AppleFilter + enable completes. Real WaitForModeB latency: 226–563ms empirically. Implemented as `HidNative.IsApplewirelessmouseInStack()`. `IsV3InModeB()` updated in `V3RecycleManager.cs`. PR #32 merged 2026-05-07. |
| H-022 | DeviceRegistry.Discover() returns col01 before col02 in Mode A | **CONFIRMED** | 2026-05-07 (Session 18) | In Mode A (FLIP:NoFilter), both col01 (mouse interface) and col02 (vendor battery collection) HID paths exist with matching VID/PID. DeviceRegistry.Discover() calls EnumerateHidPaths() and returns FirstOrDefault — SetupDi enumeration order returns col01 first. col01 has standard HID mouse UsagePage, so GetBatteryPercent() returns -1 silently (splitVendor=false, falls through to line 141). Fix: V3RecycleManager explicitly filters for col02 path by name. Evidence: All 5 readTry returned pct=-1 with no MOUSE_OPEN_FAILED or MOUSE_READ_FAILED log — silent -1 on line 141 = splitVendor=false = wrong path. Fix confirmed: pct=16 on readTry=2 after targeting col02 directly. |
| H-023 | Registry LowerFilters read is immediate and reliable for Mode B detection | **CONFIRMED** | 2026-05-07 (Session 18) | Direct read of HKLM\SYSTEM\CurrentControlSet\Enum\BTHENUM\{uuid}_VID&...\{instance}\LowerFilters via Microsoft.Win32.Registry confirms applewirelessmouse presence immediately after FLIP:AppleFilter writes the value (before disable+enable completes). No DIGCF_PRESENT dependency. Replaces both CM_Get_DevNode_PropertyW (DEVPKEY_Device_Stack, H-019 rejected) and SetupDiGetDeviceRegistryProperty SPDRP_LOWERFILTERS (also failed — DIGCF_PRESENT excludes the device during PnP reinit sequence). Evidence: V3RECYCLE Mode B confirmed restored on first FLIP:AppleFilter attempt. V3RECYCLE cycle SUCCESS pct=16 (14:34:06). Prior builds with SetupDi approach: CRITICAL after all 3 retries. |
| H-026 | applewirelessmouse+0x9f0e NULL deref BSOD trigger | **REVISED 2026-05-09** | Trigger is **aggressive HID I/O sweeping (RID-sweep probe scripts) hitting BRB cancellation paths** in IOCTL 0x410003 completion callback (per PRD-184 E_S1 static analysis), NOT the descriptor patch itself. Session 19 confirmed patched filter works fine under normal use. BSOD #1 (2026-05-07 21:17) and #2 (2026-05-08 16:15) followed probe-script activity / lifecycle disruption events that hit the cancellation/race window. Operational rule: **no RID-sweep / brute-force HID Feature probing on the live driver**. Single-shot reads only. ETW/tracelog for diagnostics, not active HID I/O. |
| H-033 | pnputil INF install with overlay-intact .sys + testsigned cat fallback FAILS Windows CI on build 26200 | **CONFIRMED via empirical test 2026-05-09** | Test: PATHA-V5-INSTALL queue route, MagicMouseFixV3.sys (78,424 B, MD5 0d9a89d0..., WHQL overlay intact, descriptor patched at 0xA850), MagicMouseFixV3.cat signed by M14 (Status=Valid). Result: pnputil /add-driver succeeded (Driver Rank 0xFF0000, oem53.inf published, "installed on device"), but device went CM_PROB_FAILED_START 0xC000009D. Get-AuthenticodeSignature on the .sys: **Status=HashMismatch** — Windows CI reads embedded PE Authenticode first, sees Apple's WHQL cert table against patched bytes, hard-blocks load. Cat fallback does NOT save it on this OS build. Forensics: C:\mm-dev-queue\forensics-20260509-212428\. **Implication**: PATH-A install must use re-sign (signtool replaces Apple WHQL with M14 sig matching patched bytes) + PATCH-APPLE-SYS direct-copy. Session 19 setup remains the working configuration. |
| H-034 | PATH-C (SDP cache patch) is NOT applicable to v3 mouse | **CONFIRMED via existing SDP dump analysis 2026-05-09** | Cross-session breakthrough: keyboard PRD-185 M2 solved via 4-byte SDP cache registry patch. Tried mapping to v3. Analysis of v3 raw SDP descriptor: declares ONLY ONE Feature (RID 0x55, 64 bytes touchpad gestures, vendor 0xFF02 TLC). col01 FeatLen=2 reported by probe-all is the **phantom RID 0x47 added by applewirelessmouse.sys at runtime in Mode B** (not in SDP cache). v3 firmware does NOT respond to ANY Feature GET_REPORT (probe-all returns err=87 for RID 0x47). Battery on v3 is ONLY readable via HidD_GetInputReport(0x90) byte[2] in Mode A. Adding Feature decls to SDP cache would create more phantom decls. **PATH-A descriptor patch is the only viable path for v3 battery.** |

## Decisions Made

| ID | Date | Decision | Rationale | Status |
|----|------|---------|-----------|--------|
| D-001 | 2026-04-17 | Use HidLibrary + HID Report 0x90, not WinRT GATT | Apple devices use proprietary HID battery reporting confirmed by WinMagicBattery reverse engineering | active |
| D-002 | 2026-04-22 | worst-state-wins semantics for DriverHealthChecker | Multiple Magic Mouse HID devices share same Vendor ID — if any one is unbound, the whole device is in broken state | active |
| D-003 | 2026-04-21 | Do NOT use ReportDescriptor as binding signal (H-001) | Too many false negatives — descriptor can be null even when driver is bound | active |
| D-014 | 2026-04-28 | M12 clean-room KMDF filter driver selected over shipping MU driver as production | APPROVED. Clean IP, no EULA shipping risk, no trial-expiry dependency, no userland maintenance debt. MU trial expired (app dead); shipping MU's binary would require ongoing license compliance with no upgrade path. M12 is ~50 LOC translation logic + ~400 LOC scaffold, estimated 4-8 hr agentic with Ghidra reference. See `docs/M12-MAGIC-UTILITIES-REFERENCE-PLAN.md`. | active |
| D-015 | 2026-04-28 | v1 Magic Mouse (PID 0x030D) adopted as known-good control for M12 regression validation | APPROVED. v1 firmware backs Feature 0x47 natively; v1 + v3 HID descriptors are byte-identical post-filter. M12 INF must bind both PIDs. v1 must produce `OK battery=N% (Feature 0x47)` on every M12 build iteration before v3 is tested. v1 regression failure = broken pass-through path; diagnose independently from v3 translation failure. | active |
| D-016 | 2026-04-28 | M12 architecture = pure kernel filter, ~300-500 LOC, no userland split | APPROVED 2026-04-28 (Session 12). Architecture: replicate MU's Mode A descriptor mutation + implement in-IRP scroll/battery translation derived from Linux `hid-magicmouse.c` reference + Ghidra analysis of `MagicMouse.sys`. No userland service dependency. Empirical basis: H-013 CONFIRMED FAIL — kernel-only MU install (trial-expired userland dead) breaks scroll and hides battery, proving userland-split design is unnecessary complexity that creates trial-expiry and maintenance risk. | active |
| D-017 | 2026-04-30 | M13 scope is complete: descriptor injection only. Gesture processing (scroll) is M14. | APPROVED 2026-04-30 (Session 13). M13 delivered: (1) SDP IOCTL interception via EvtIoDeviceControl, (2) combined 135-byte HID descriptor (delta=0 in-place swap), (3) COL01+COL02 both enumerated (Started), (4) LowerFilters persistence fix (post-restart re-apply), (5) sc.exe 1072 race fix, (6) empirical proof SdpPatchSuccess=1 IoctlInterceptCount=2. Scroll blocked by missing gesture processing — confirmed NOT solvable via applewirelessmouse coexistence (H-016 REJECTED). M14 scope: intercept RID=0x27 completions in MagicMouseDriver, parse Apple multi-touch format (Linux hid-magicmouse.c blueprint), synthesize RID=0x02 scroll events. | active |
| D-018 | 2026-04-30 | applewirelessmouse.sys remains in LowerFilters at index 1 (above MagicMouseDriver) but is excluded from gesture processing role | APPROVED 2026-04-30 (Session 13). applewirelessmouse stays in stack because removing it could break unknown PnP/power functions. It is harmless at index 1 — it sees our combined descriptor already patched and does not re-patch (confirmed: COL01+COL02 remain enumerated with applewirelessmouse above our driver). Its gesture processing, while active when at index 0, conflicts with our combined descriptor. At index 1 it is effectively dormant for gesture purposes. | active |
| D-S17-26 | 2026-05-09 | PATH-A v5 INF install path with overlay-intact .sys is REJECTED on Win11 26200 — pivot to Session 19 direct-copy + re-sign | Empirical test confirmed Get-AuthenticodeSignature Status=HashMismatch on patched .sys → CI hard-block 0xC000009D. Session 19 (2026-05-08) used signtool re-sign on the .sys (strips Apple WHQL, writes new M14 sig matching patched bytes) + PATCH-APPLE-SYS direct-copy queue route — confirmed working (battery delivered, cursor+scroll). PATH-A v5 forward path: SIGN-FILE → CLEAR-BT-SDP-CACHE → PATCH-APPLE-SYS → RESTART-DEVICE → verify. See `docs/PATH-A-V5-CI-HASHMISMATCH-FINDING.md`. | active |
| D-S17-27 | 2026-05-09 | Operational hygiene: NO RID-sweep / brute-force HID Feature probe scripts on the live driver | Per H-026 revised: BSOD trigger is BRB cancellation in IOCTL 0x410003 completion path (E_S1). Aggressive HID probing exposes the latent NULL-deref. Single-shot reads only. Diagnostics via ETW/tracelog (read-only), not active HID I/O. | active |
| D-S17-28 | 2026-05-09 | mm-task-runner.ps1 framework expanded with new routes for v5 + recovery primitives | Added: PATHA-V5-INSTALL (deprecated by D-S17-26 in favor of direct-copy chain), PATHA-V5-UNINSTALL, DISABLE-ENABLE-DEVICE (force PnP re-bind), SET-LOWERFILTERS (write LowerFilters value with backup + restart-device). All recovery actions go through queue, no one-off scripts. | active |
| D-S17-29 | 2026-05-09 | M14 gesture engine acceleration enabled by MacBook + PacketLogger access | PacketLogger captures on MacBook with paired v3 give wire-level ground truth for RID 0x27 multi-touch byte layout. Eliminates the "research" phase from M14. Estimated 4-7 sessions (1-2 weeks) for shippable scroll vs prior 4-8 week estimate. M14a discovery via PacketLogger captures, M14b translation logic from Linux + Ghidra reference, M14c kernel wiring on existing M13 scaffold, M14d polish. | active |
| D-S17-30 | 2026-05-09 | PATH-C (SDP cache patch) confirmed NOT applicable to v3 — keyboard PRD-185 breakthrough does not translate | Per H-034: v3 firmware-protocol asymmetry (only RID 0x55 declared as Feature, no firmware response to any Feature GET_REPORT). PATH-A descriptor patch remains the only viable v3 battery path. | active |

## Next Session Brief

Session 18 COMPLETE: Full PATH-B pipeline working end-to-end.
V3RECYCLE cycle SUCCESS pct=16 confirmed 2026-05-07 14:34:06.

Fixes implemented in Session 18:
- H-022: Col02 path targeting in V3RecycleManager (bypasses DeviceRegistry.Discover())
- H-023: Registry-based Mode B confirmation (Microsoft.Win32.Registry, no SetupDi)
- Cursor-position idle detection (replaces GetLastInputInfo — system-wide events blocked it)
- ForceReadNow menu item added (bypasses idle wait)
- DrainRateTracker: per-device drain rate estimation, adaptive cadence
- Keyboard battery read diagnostic logging added

Session 19 priorities:
1. Confirm keyboard battery read fix (KB_CAPS_FOUND logging → identify correct path)
2. PATH-A signing: Invoke-Signing thumbprint 16940C0F937D569363560D5FEC5CD8FA6D6D9BCE
3. Resolve pre-existing merge conflicts: driver/Driver.c, driver/Driver.h, scripts/mm-dev.ps1
4. Full PR with all M3 Release changes

**Do NOT retry from pre-Session-14 state below — superseded**:

---

**Archived brief (2026-05-01 post-Session-14)**:
- **Reboot pending** — PFRO queued to restore original `applewirelessmouse.sys` (78424B). V3 binary patch in place now causes spurious clicks + no scroll; PFRO replaces it on next boot.
- **startup-repair.ps1 running** — boot-time scheduled task confirmed active (log 2026-04-30 08:18:21 repaired=True). Runs at 30s post-login: detects COL02 missing → removes LowerFilters → disable+enable BTHENUM → Descriptor A (COL01+COL02) → restores LowerFilters to Enum key + driver instance key → no /restart-device.
- **Target state post-reboot**: COL01 (scroll) + COL02 (battery) both present. HidD_GetInputReport(0x90) buf[2] = battery %. This is the same state S13 confirmed on 2026-04-30.
- **M13 KMDF driver (MagicMouseDriver) installed** — LowerFilters includes both MagicMouseDriver and applewirelessmouse. MagicMouseDriver handles SDP descriptor injection. Scroll synthesized by applewirelessmouse at LF[1].
- **V4 patch (standby)**: `D:\Backups\AppleWirelessMouse-RECOVERY\AppleWirelessMouse-v4.sys` — 116B descriptor (2 buttons, RID=0x27, COL02 TLC), signed. Use if startup-repair approach fails.

**Session 15 first action**:
1. Confirm reboot happened. Run state capture (`capture-state.ps1`) → verify COL01=True, COL02=True.
2. Run battery read test: `HidD_GetInputReport(0x90)` on COL02 → buf[2] should be battery %.
3. If verified: Session 14 close-out gate passed. Proceed to M14 (scroll gesture translation).
4. If COL02 missing: check `C:\ProgramData\MagicMouseTray\startup-repair.log` for failure reason.

**Key M13 discoveries (from S13 + S14)**:
1. **applewirelessmouse.sys IS a kernel gesture processor** — At LF[0] causes spurious clicks (descriptor mismatch). At LF[1] dormant for gesture; scroll synthesis works when Descriptor A is in effect.
2. **startup-repair.ps1 supersedes V4 binary patch** (D-M13-04). Boot-time BTHENUM cycling achieves COL01+COL02 without patching Apple's binary.
3. **V3 binary patch pitfalls**: changing button count (`29 02` → `29 05`) breaks mouhid. Removing RID=0x27 breaks scroll synthesis. Any binary patch must preserve these exactly.
4. **signtool path**: `C:\Program Files (x86)\Windows Kits\10\bin\10.0.26100.0\x64\signtool.exe` — corrected in mm-task-runner.ps1.

**Files to read first (M15 or M14 scroll)**:
- `scripts/mm-dev.ps1` — Install-Driver + Restore-LowerFilters phases (branch: `ai/m13-s13-sdp-inject`)
- `driver/MagicMouseDriver.c` — SDP IOCTL interception + completion routine
- `startup-repair.ps1` (worktree `ai-m13-s13-sdp-inject`) — boot-time COL02 repair mechanism
- Linux kernel `drivers/hid/hid-magicmouse.c` — RID=0x27 Apple multi-touch format decoder (M14 blueprint)
- This PSN file — all hypotheses H-001..H-018 and decisions D-001..D-018 + D-M13-04

**M14 scope (scroll gesture translation — deferred until reboot verified)**:
1. Add WDF EvtIoRead completion callback to intercept READ IRP completions from HidBth
2. On completion, check RID byte 0 == 0x27 (multi-touch report)
3. Parse finger count, X/Y deltas, pressure using Linux hid-magicmouse.c as format reference
4. Synthesize RID=0x02 scroll events (Usage 0x38 Wheel + Usage 0x038B AC Pan) and inject into IRP buffer before completing upward
5. **First M14 session gate**: DebugView logging of raw RID=0x27 bytes → verify v3 byte layout vs Linux driver expectations
6. Estimated: 3-4 sessions

**Do NOT retry**:
- H-001 ReportDescriptor as binding signal | H-003 `pnputil /restart-device` near active applewirelessmouse | H-004 LowerFilters in driver instance key | H-005 filter-binding-unreliable | H-008 "Apple traps Feature 0x47" | H-015 descriptor injection alone sufficient for scroll | H-016 applewirelessmouse coexistence for gesture (descriptor conflict is fatal)
- All pre-M13 MU/preserve-INF paths — superseded by clean-room M13 driver

**Anti-patterns added Session 13**:
- AP-25 — applewirelessmouse.sys coexistence at LF[0] is a dead end. Its gesture engine initializes against the SDP descriptor present at startup; a later descriptor overwrite causes gesture output bytes to land in wrong HID fields (button bits → spurious clicks). Never retry this approach.
- AP-26 — Never test a filter order swap without a rollback commit ready and the expected behavior documented beforehand. Spurious-click behavior from the ee18af4 swap was confusing without prior documentation of what the test was measuring.

**GitHub Issues**: #2 (suspend-resume), #3 (BTHENUM timing partially addressed by LowerFilters persistence fix), #4 (driver priority logic — scroll component deferred to M14).

## Session History

| Date | Session | What Happened |
|------|---------|--------------|
| 2026-04-17 | Session 1 | Initial feasibility + HID 0x90 discovery. All 7 milestones completed. |
| 2026-04-21 | Session 2 | Peer review via NotebookLM. 3 blocking bugs filed as Issues #2, #3, #4. H-001 rejected. |
| 2026-04-22 | Session 3 | H-002 confirmed (worst-state-wins). PR merged. |
| 2026-04-24 | Session 4 | H-003 opened (pnputil restart safety). PSN created. |
| 2026-04-26 | Session 5 | test-filter-stack.ps1 run. H-003 rejected (/restart-device strips COL02). H-004 rejected (driver instance key write doesn't fix 1077 — tested in prior sessions). startup-repair.ps1 fixed (Bugs 1+2). KMDF driver confirmed as only path for scroll. |
| 2026-04-27 | Session 6 | Full AppleWirelessMouse.inf (DriverVer 04/21/2026 6.2.0.0) reviewed. INF confirms error 1077 root cause: HKR in .HW.AddReg = Enum key only; PnP skips AddDevice on BTHENUM reuse at boot. Function driver INF design confirmed: SPSVCINST_ASSOCSERVICE, no Include=hidbth.inf, same four hardware IDs. Pre-build validation complete (TouchpadProbe, descriptor, stack baseline, .sys hash). KMDF-PLAN.md updated with INF analysis. |
| 2026-04-27 | Session 7 | M13 Phase 0 + Phase 1 executed. Three orphans cleared (MagicMouseDriver svc key, USB MI_01 LowerFilters="MagicMouse", MAGICMOUSERAWPDO PnP node). Two cleanup-script bugs fixed (B1 health check, B2 `\\` pattern). Reg-diff verification baked in as MOP gate (mm-reg-diff.sh, mm-phase1-closeout.sh). Plan v1.1, playbook v1.1 (+AP-12/13/14/15). Pre/post .reg backups + diff audit confirm only expected mutations. Halted at Phase 2 gate pending G1 decisions #2/#3/#4. |
| 2026-04-27 | Session 8 | M13 Phase 2 Cell 1 (T-V3-AF) executed end-to-end. test-1 -> unpair -> repair -> test-2 -> sleep/wake -> test-2b -> reboot -> test-3. Post-reboot: pointer OK, battery OK (44%), scroll broken, AC-Pan broken. wheel-events.json event_count=0 over 3-sec gesture. Live probe: applewirelessmouse IS bound (LowerFilters on HID-class GUID confirmed) and service IS loaded -- earlier hypothesis "filter unreliable across reboots" is WRONG. Real cause: COL01 HID descriptor declares only X (Usage 0x30) + Y (0x31), NO Wheel (0x38) -- even with Apple filter, HidClass has no value cap to deliver wheel events through. Phase 4A (userland scroll daemon) trending viable; 4C (descriptor patch) alternative. Caveats: mm-accept-test AC-01 script bug -- queries SDP-service GUID {00001200-...} where LowerFilters never lives instead of HID-class GUID {00001124-...}; ignore that PASS/FAIL until script fixed. Pre-reboot ETW 14.5 GB; post-reboot ETW 1.65 GB; 9 unfiltered Procmon Bootlog rollover files at ~3 GB each (filter v1 not active for cell 1 -- wired in for cells 2-6). Block 1 close-out complete (reg-diff + state snapshot). Cell 1 report: .ai/test-runs/2026-04-27-154930-T-V3-AF/cell1-report.md. |
| 2026-04-27 | Session 9 | M13 Phase 3 BTHPORT cache decode + independent multi-agent forensic analysis on Cell 1. Phase 3 reveals cache has 135-byte HID descriptor with Mouse(X+Y, NO Wheel) + Vendor Feature + Vendor Battery (Report 0x90). Q1=YES Q2=NO. REFUTES Agent A + Agent C cache hypotheses. Independent analysis pipeline ran 3 blind agents + synthesis -- caught 2 material errors in original Cell 1 conclusions (M-1 Q7 trending YES via 4A claim was premature; M-2 kernel-debug-tail content was stale duplicate from one earlier window). applewirelessmouse must do scroll synthesis at Win32 input layer (Linux hid-magicmouse.c pattern), NOT via descriptor augmentation. Q7 answer: YES via Phase 4A. Bug found in mm-bthport-read.ps1 path assumption (CachedServices\00010000 is a value name, not subkey) -- discover.ps1 written as workaround. Tooling fixes landed: AC-01 GUID fix, kernel-log rotation, m13.wprp focused ETW profile (commit ce0dd18). Phase 3 commit 6b4453e. |
| 2026-04-27 | Session 10 | Phase 4-Omega discovery + persistence-monitor + state clarification. Disable+Enable BTHENUM HID device reliably flips State B (filter inert, scroll broken, battery readable) -> State A (filter active, scroll works, battery N/A). 65-min persistence monitor confirmed State A held idle without degradation. CRITICAL CLARIFICATION: Phase 4-Omega restores State A which is the SAME state user is in steady-state -- it auto-recovers from the regression but does NOT solve the mutual-exclusion of scroll vs battery. PRD-184's primary feature (battery readout) requires Phase 4A (userland scroll daemon, NoFilter mode) or Phase 4C (cache patch) -- these put the device in State D or E where BOTH work simultaneously. Phase 4-Omega alone is insufficient for PRD-184. State table documented in PHASE4-OMEGA-PLAN.md and the user's chat history. C# prototype committed (e323df6) but NOT YET WIRED into TrayApp.cs pending user approval on path. Open questions: keyboard's behavior (subagent extracting), v1 mouse comparison (user pairing now), the actual trigger of State A->B (Selective Suspend H-alpha still unconfirmed but moot given Phase 4-Omega sidestep). |
| 2026-04-28 | Session 11 | Phase E + research-driven empirical findings (commit 994c66d). MAJOR narrative corrections: (1) Two-state descriptor model — H-010 confirmed: HidBth serves either Descriptor A (vendor 0xFF00 TLC enumerated as COL02, battery readable, 96 OK reads on 04-27) or Descriptor B (single Mouse TLC + phantom Feature 0x47, battery err=87, 295 FEATURE_BLOCKED). Registry byte-identical between states; cache lives in kernel pool. (2) Filter binding is NOT the axis — `DEVPKEY_Device_LowerFilters` confirms `applewirelessmouse` is bound to v1+v3 in BOTH states. Earlier `bt-stack-snapshot.txt` reading the wrong registry path was the bug, not filter binding (AP-18). (3) "Apple driver traps Feature 0x47" framing (H-008, MouseBatteryReader.cs log line, PRD narrative) is INFERENTIAL not measured — REJECTED. err=87 just means the device doesn't back the phantom 0x47 (AP-19). (4) ~588 probe attempts across 6 channels (PnP DEVPKEY × 24, WMI battery × 5, AppleBluetoothMultitouch IOCTL 0x800-0x830 × 49, HidD_Get* every ReportID × 508, orphan COL02 direct open, Get-PnpDevice -Class Battery) → 0 hits in Descriptor B. (5) DSM property-write event at 04-27 19:50:53 correlates with the only observed A→B flip (H-011 partial). (6) Magic Utilities WAS bound to v3 from 2026-03-18 → 2026-04-17 with `LowerFilters: MagicMouse` per registry from 2026-04-03 backup; user has ruled out re-purchasing. Preserve-INF plan + capture script (663 lines) documented as last resort but EULA-questionable. (7) NotebookLM 150-source deep research synthesis (notebook bd78726f-…) — three-channel battery taxonomy (BLE Manuf Data / GATT 0x180F / HID), MagicUtilities-polls-15-min cadence guidance, OSS reference repos (WinMagicBattery / martinsoft / Magic-Warnings / mac-precision-touchpad). Anti-patterns added to playbook v1.6: AP-18 wrong registry path, AP-19 single-error-code causal inference, AP-20 screenshot layout described not data extracted. Path-forward decision pending: Option Z (detect B + recycle, 1-2 days tray code) vs Phase M12 KMDF filter (2-4 weeks) vs Path 4 IOCTL RE on `/tmp/applewirelessmouse.sys` (1-2 days RE) vs Path 5b preserve-INF (last resort). Recycle test on v3 NOT YET RUN. |
| 2026-04-28 | Session 12 (early) | PRD-184 plan corrected. **Decision D-S12-01** (D-S12 = Session 12 decision): Use Magic Utilities as **reverse-engineering reference only, NOT production install**. (D-S12-02) Build clean-room **M12 KMDF filter driver** from captured material. (D-S12-03) **Uninstall MU after capture** phase. (D-S12-04) Adopt **v1-as-control strategy** for validation. Rationale: cleaner IP, no EULA shipping risk, no trial-expiry dependency, no userland maintenance debt (MU trial now expired, app unmaintained). OLD plan (install MU, capture INF, ship MU driver as production) reframed as reference-only; new plan is custom clean-room implementation. Two anti-patterns captured: AP-22 (don't ship third-party trial driver as production) + AP-23 (don't manipulate trial registry to extend expiry). Playbook bumped to v1.8. Pending: Windows 11 Home → no Sandbox → install on host; setup.exe run; capture script execution; Ghidra analysis of extracted .sys; M12 implementation starting from captured material; regression validation (v1 as control baseline). |
| 2026-04-28 | Session 12 (continued) | **Kernel-only MU test executed and conclusive (H-013 CONFIRMED FAIL).** Empirically confirmed that MU's kernel filter `MagicMouse.sys` does descriptor mutation only — scroll translation and battery queries are gated by userland service `MagicUtilitiesService.exe`. With trial-expired userland (service dead), scroll broken on both v1 and v3 and battery hidden. **M12 architecture finalized (D-016)**: pure kernel filter, ~300-500 LOC, replicates MU's Mode A descriptor + does in-IRP translation derived from Linux `hid-magicmouse.c` + Ghidra on `MagicMouse.sys`. No userland split. **Captures created**: `D:\Backups\MagicUtilities-Capture-2026-04-28-1937\` (41 files, 78.6 MB) including driver-package, driver-binary, program-files (userland), registry exports, PnP topology, sc qc text dumps. HID descriptor dumps in `.ai/test-runs/2026-04-27-154930-T-V3-AF/hid-descriptor-full-{kbd-col01,v1-col01,v3-col01}.txt`. **Incident — destructive command without backup**: `pnputil /delete-driver oem0.inf /force` (Apple's INF) executed to make MU's INF win the PnP rank battle without prior backup; Apple Wireless Mouse driver lost. Recovery via prior session snapshots (`.ai/snapshots/mm-state-*/oem0-applewirelessmouse.inf`) and user's downloaded zip (`D:\Users\Lesley\Downloads\MagicMouse2DriversWin11x64-master.zip`). Permanent recovery backup staged at `D:\Backups\AppleWirelessMouse-RECOVERY\`. Incident doc: `docs/INCIDENT-2026-04-28-APPLE-INF-DELETION.md`. Recovery procedure: `docs/APPLE-DRIVER-RECOVERY-PROCEDURE.md`. **New permanent feedback rule**: AP-24 + `~/.claude/projects/-home-lesley-projects/memory/feedback_backup_before_destructive_commands.md`. PSN-0001 v1.8.0 → v1.9.0. |
| 2026-04-30 | Session 13 | **M13 KMDF lower-filter driver complete.** SdpPatchSuccess=1, IoctlInterceptCount=2. Combined 135-byte HID descriptor injected on BTHENUM enumeration. COL01+COL02 both Started. LowerFilters persistence solved (post-restart re-apply + -Phase Restore). sc.exe 1072 race fixed. **Scroll root cause confirmed**: v3 sends raw RID=0x27 multi-touch (46 bytes, Apple proprietary) — not standard HID scroll values. Gesture translation required in M14. **LowerFilters swap test** (commit ee18af4): confirmed applewirelessmouse.sys IS a kernel gesture processor when at LF[0] — produces spurious left-clicks due to descriptor conflict (H-016 REJECTED). Reverted (commit f102777). applewirelessmouse remains at LF[1] (dormant, harmless). **H-017 CONFIRMED**: SDP = IRP_MJ_DEVICE_CONTROL. **H-018 CONFIRMED**: WDF filter requires explicit EvtIoDefault. **Decisions D-017 + D-018 recorded**. AP-25 + AP-26 added. PSN-0001 v1.9.0 → v2.0.0. M14 scope documented. Branch: `ai/m13-s13-sdp-inject` (commits 920b589..f102777 pushed). |
| 2026-05-07 | Sessions 15-17 | **PATH-A binary patch design, peer review, MOP finalization.**
Session 15 (2026-05-05): M0 validation `test-v3-state.ps1` on hardware. COL02 `DN_STARTED` at ~3ms; attempt 1 `HidD_GetInputReport(0x90)` GLE=121 (ERROR_SEM_TIMEOUT) resolved by 500ms retry; attempt 2 BATTERY=18%, total 29s, PASS. Confirmed `DEVPKEY_Device_Stack` as authoritative Mode B discriminator; `IsV3MouseClassPresent` DN_STARTED gate was false positive. H-012 test: recycle from Mode B, 100 attempts, success rate classified for PRD-26 M0 gate.
Session 16 (2026-05-06): Ghidra capstone disassembly of `applewirelessmouse.sys` F3 callback (VA `0x14000a440`). **H-019 CONFIRMED**: two SSE patch sites (file `0x9611` + `0x96e5`), both copy 116 bytes via 7×`movups xmm,[rip+disp]` + 1×DWORD. Source: file `0xA850`–`0xA8C4` (RVA `0xC050`). Apple's filter has NO Mode A/B branching. H-010 RE-REVISED: Mode mutual exclusivity lives in HidBth downstream cache, not Apple's filter. PATH-A descriptor designed: TLC1=89B (remove Feature 0x47 + vendor 1-bit pad, change 5→6-bit Const) + TLC2=27B (vendor 0xFF00, RID=0x90, ReportCount=2). Peer reviews: NLM T3 APPROVE (`9a96fef1`) — fatal flaw found (ReportCount=3→2 fixed), all 5 hypotheses verified. Gemini CHANGES-NEEDED → applied (vendor pad removal + 2-TLC gesture engine = runtime verification items). Candidate v3 built: MD5 `0d9a89d08ccc89f46f47775e72c80d7b`, staged to `C:\mm-dev-queue\`.
Session 17 (2026-05-07): Expert adversarial review of MOP install-pathA-candidate.ps1; 4 bugs fixed: (1) step 5h replaced broken `mm-hid-feature-read.ps1` call with inline C# `HidBatteryReader::ReadV3Battery` (exact from test-v3-state.ps1) + 5×/500ms retry; (2) MD5/SHA256 updated to v3 unsigned values; (3) LogMax=255 comment clarified for raw-read path; (4) peer review header updated to NLM APPROVE + Gemini applied. PR #32 (PATH-B Mode B detection fix via DEVPKEY_Device_Stack) reviewed cross-session and merged via gate. Local main synced to `9ccf2e9`. M13/M14 stash-pop conflict markers in Driver.c/Driver.h/mm-dev.ps1/mm-task-runner.ps1 resolved. PSN v2.1.0 → v2.2.0. |
| 2026-05-02 | Session 14 | **WDF filter HID descriptor fixed + battery confirmed + permanent install.** Root cause of 0xC00000B9 (CM_PROB_FAILED_START / STATUS_COULD_NOT_INTERPRET): TLC-2 and TLC-3 in the injected descriptor had Input items with no Usage declaration and no Logical Min/Max -- hidclass.sys rejects these with STATUS_COULD_NOT_INTERPRET during IRP_MN_START_DEVICE. Isolated via EnableInjection=0 passthrough test: passthrough -> HID OK=2 (Works); injection -> HID OK=0 (Fails). Filter code confirmed correct; descriptor was sole cause. **HidDescriptor.c v2 (135 bytes, delta=0)**: removed 11-byte vendor 1-bit pad from TLC-1 (89 bytes), added Usage+LogMin/Max to TLC-2 (22 bytes), added UsageMin/Max+LogMin/Max to TLC-3 (24 bytes), removed trailing 3x 0x00 reserved Main items. Binary-patched MagicMouseDriver-wdf.sys at offset 0x4450. **Signing**: signtool failed (private key not accessible to SYSTEM); used Set-AuthenticodeSignature with freshly-created CN=MagicMouseFix cert created AS SYSTEM. UnknownError expected for self-signed; TrustedPublisher install satisfies CI in testsigning mode. **Result**: HID OK=3: COL01=mouse/mouhid OK, COL02=battery vendor OK, COL03=touch vendor OK. Diag: IoctlInterceptCount=2, SdpPatchSuccess=1, LastPatchStatusHex=0, LastSdpBufSize=273. **Battery confirmed**: COL02 HidD_GetInputReport(RID=0x90) -> buf[2]=0x18=24%. Permanently installed (V4 no longer in use). AP-27: Check for non-ASCII in WSL-created PS scripts (em-dash causes silent parse failure). AP-28: Set-AuthenticodeSignature preferred over signtool when running as SYSTEM. PSN-0001 v2.0.0 -> v2.1.0. |
